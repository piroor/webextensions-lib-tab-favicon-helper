{
  "name": "webextensions-lib-tab-favicon-helper",
  "version": "0.0.0",
  "engines": {
    "node": ">=8.6.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "eslint": "^7.18.0"
  }
}
